{
  "name": "eth-wallet-address-master",
  "version": "1.0.0",
  "description": "<h5> This is an application that uses Infura as the API call to Generate a Ethereum Wallet Address.  Then take the Ethereum Wallet Data and parse it over via JSON to the MongoDB. This concept can be applied to many different applications involving generating Decentralized Data and storing that Data via Centralized.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.2",
    "express": "^4.17.3",
    "mongoose": "^6.2.4",
    "web3": "^1.7.0"
  }
}
